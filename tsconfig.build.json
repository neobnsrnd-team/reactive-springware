{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* 빌드 시에는 emit이 필요하므로 noEmit 해제 */
    "noEmit": false,
    /* allowImportingTsExtensions은 noEmit:true 전용 옵션 — 빌드 tsconfig에서는 제거 */
    "allowImportingTsExtensions": false,
    "declaration": true,
    "emitDeclarationOnly": true
  }
}
